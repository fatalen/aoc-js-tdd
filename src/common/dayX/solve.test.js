import { expect, test } from 'vitest'
import { part1, part2 } from './solve'

test('part1 example', () => {
  expect(part1('test')).toBe(0)
})

test('part2 example', () => {
  expect(part1('test')).toBe(0)
})